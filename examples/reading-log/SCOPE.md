# Reading log — agreed scope

**Tracking:** committed deliberately — a shipped example, written from an invented scope. Public
repository. A real run's artifacts are untracked by default; see `references/artifacts.md`.

Settled over 2 rounds. Every line below is the slot's own text, verbatim, with the answers that
earned it. `node ../../skills/megascope/assets/megascope.mjs ready .` exits 0 on this directory.

## What we're trying to achieve

> A log you keep up: title only, logged in a tap or two, and it works in a tunnel.

From `r1:Q1` (a habit over a permanent record), `r1:Q2` (title only, changed from the
recommendation), `r2:Q6` (saves on the phone, syncs later).

## What we hand over

> A one-page build plan, readable on the train, that says what to build and what not to build.

Kind: **new-project-plan**. From `r1:Q4` — answered in the user's own words, which superseded the
option offered: *"a plan is right but I want it short enough to read on the train too. one page.
and tell me what NOT to build, that is the bit I always get wrong."* Plus `r1:Q5` (a sketch of the
logging screen).

## What's in and what's out

> Finished and abandoned books both go in. Entries can gain fields later. Still nothing social.

From `r2:Q7`, `r2:Q8`. The abandoned-books question came from the user's own overall note in round
1 — nothing in round 1 had asked about it.

## How we know it's done

> Three taps or fewer to log a book, checked on day one, then still in use a month later.

From `r2:Q9` (changed: both tests, in that order) and `r2:Q10` (a copy kept off the phone).

## Fixed facts we work inside

> Phone, on the train, under six taps to log a book. No account, no feed, nobody else ever sees it.

From `r1:Q1` and the round-1 overall notes.

## Decided by assumption, not agreement

None. Round 1 settled `boundary`, `verification` and `constraints` by assumption; round 2 reopened
the first two and asked about them properly, and `constraints` picked up real evidence on the way.

## How it got here

| Round | Asked about | What moved |
|---|---|---|
| 1 | goal, deliverable | deliverable settled. Q3 was **rejected** — the offline question was asked in terms that meant nothing. |
| 2 | goal, boundary, verification | all three settled. Q6 re-asked the rejected question in plainer words, with an everyday example, and it was answered on the default. |

The rejection is the useful part of this record. Round 1's Q3 asked *"Should it work with no
signal?"* and the user replied that they could not tell the two options apart. Round 2 asked
*"In a tunnel, what happens when you tap save?"* with a concrete example, and it was answered
first time. The question was wrong, not the reader.
